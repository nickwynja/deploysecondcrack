## Install Apache, PHP, iNotify, Git
    
    yum install httpd php python gcc automake autoconf libtool make && chkconfig --levels 235 httpd on && wget --no-check-certificate http://github.com/downloads/rvoicilas/inotify-tools/inotify-tools-3.14.tar.gz && tar xvfz inotify-tools-3.14.tar.gz && cd inotify-tools-3.14 && ./configure --prefix=/usr --libdir=/lib64 && make && su -c 'make install' && rpm -Uvh http://repo.webtatic.com/yum/centos/5/latest.rpm && yum install --enablerepo=webtatic git
  
     wget http://developer.axis.com/download/distribution/apps-sys-utils-start-stop-daemon-IR1_9_18-2.tar.gz; tar zxvf apps-sys-utils-start-stop-daemon-IR1_9_18-2.tar.gz; cd apps/sys-utils/start-stop-daemon-IR1_9_18-2/; gcc start-stop-daemon.c -o start-stop-daemon
  
## Switch to 'blog' account

    useradd -d /home/blog -m blog && sudo passwd blog
    visudo
    su blog

## Install Dropbox
    
    cd ~ && sudo mkdir bin && cd bin && wget http://www.dropbox.com/download?dl=packages/dropbox.py

    cd /home/blog/ && wget -O - http://www.dropbox.com/download?plat=lnx.x86 | tar xzf - && /home/blog/.dropbox-dist/dropboxd
    
## Install SecondCrack

  cd /var/www/ && sudo git clone git://github.com/marcoarment/secondcrack.git
    
## Config SecondCrack

    sudo cp /var/www/secondcrack/config.php.example /var/www/secondcrack/config.php && sudo vi /var/www/secondcrack/config.php
    
##  Set up Update

    sudo cp /var/www/secondcrack/config/example.crontab /etc/crontab
    vi /etc/crontab
    * * * * * /var/www/secondcrack/engine/update.sh /home/blog/Dropbox/Blog /var/www/secondcrack
    
## Apache Config

    sudo vi /etc/httpd/conf/httpd.conf
    
    DocumentRoot "/var/www/secondcrack/www"
    <Directory />
      Options FollowSymLinks
      AllowOverride All
    </Directory>

    <Directory "/var/www/secondcrack/www">
    
    sudo service iptables stop

vi /etc/php.ini
    short_open_tag

 276 ServerName 50.57.151.190:80
 
 
# cd /usr/srcwget http://rfxnetworks.com/downloads/apf-current.tar.gz
# tar -xvzf apf-current.tar.gz
# rm -f apf-current.tar.gz 
# cd apf-*
# ./install.shcd /etc/apf